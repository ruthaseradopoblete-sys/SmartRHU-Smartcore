import './globals.css'
export const metadata = { title: 'SmartRHU – Municipal Health Office', description: 'Lopez, Quezon' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
